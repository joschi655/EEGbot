A small mono metadata token — filters, §-references, attributes. Lower emphasis than Badge.

```jsx
<Tag>§ 19 EEG</Tag>
<Tag accent>Photovoltaik</Tag>
<Tag onRemove={() => removeFilter('overdue')}>Überfällig</Tag>
```

Props: `accent`, `onRemove` (renders a removable ✕).
