A user/entity avatar — image or initials fallback.

```jsx
<Avatar name="Lena Brandt" />
<Avatar name="Klima Energie GmbH" size="sm" accent />
<Avatar src="/uploads/lena.jpg" name="Lena Brandt" size="lg" />
```

Props: `name`, `src`, `size` (`xs`/`sm`/`md`/`lg`), `accent`.
