A short hover/focus tooltip wrapping a trigger.

```jsx
<Tooltip label="Letzte Prüfung: vor 4 Std." side="top">
  <IconButton label="Info" icon={<i data-lucide="info" />} />
</Tooltip>
```

Props: `label`, `side` (`top`/`bottom`/`left`/`right`). Keep to one line.
