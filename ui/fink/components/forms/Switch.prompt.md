An on/off toggle for instant settings (no save step). Klein-blue when on.

```jsx
<Switch label="E-Mail-Benachrichtigungen" defaultChecked />
<Switch label="Wochenend-Erinnerungen" />
```

Props: `label`, `disabled`, plus native checkbox attributes (`checked`, `onChange`…).
